replicaCount: 1

image:
  repository: ghcr.io/lucientong/waggle
  tag: "0.3.0"
  pullPolicy: IfNotPresent

service:
  type: ClusterIP
  port: 8080

resources:
  limits:
    cpu: 500m
    memory: 128Mi
  requests:
    cpu: 100m
    memory: 64Mi

env: []
#  - name: OPENAI_API_KEY
#    valueFrom:
#      secretKeyRef:
#        name: waggle-secrets
#        key: openai-api-key
